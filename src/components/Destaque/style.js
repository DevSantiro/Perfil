import styled from 'styled-components';

export const LIDestaque = styled.li`
    background-color: var(--cor-contraste-claro);
    border-radius: 10px;
    box-shadow: var(--sombra);
    width: 100%;
    max-width: 15rem;
    padding: 1.75rem;
    box-sizing: border-box;
    margin-bottom: 1rem;
`;